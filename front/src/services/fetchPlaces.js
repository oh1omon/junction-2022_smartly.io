import axios from 'axios'

export const fetchPlaces = () => {
    // axios(config)
    // .then((res) => {
    //   const resPlaces = res.data.results.slice(0, 4)
    //   setPlaces(resPlaces)
    // })
    // .catch((error) => {
    //   console.log(error);
    // });
    return []
}